# Инструкция по диагностике проблемы на мобильных устройствах

## 🔍 Диагностика проблемы

### 1. **Откройте консоль разработчика на мобильном устройстве:**

#### Для iPhone (Safari):
1. Откройте Safari на iPhone
2. Перейдите в **Настройки** → **Safari** → **Дополнительно**
3. Включите **Веб-инспектор**
4. Подключите iPhone к Mac
5. На Mac откройте Safari → **Разработка** → выберите ваш iPhone
6. Откройте консоль разработчика

#### Для Android (Chrome):
1. Откройте Chrome на Android
2. Перейдите в **chrome://inspect**
3. Включите **Удаленная отладка**
4. Подключите к компьютеру через USB
5. Откройте консоль разработчика

### 2. **Проверьте статус backend:**
- В левом верхнем углу приложения появится индикатор статуса backend
- Если статус "Ошибка подключения" - проблема в сети или backend недоступен

### 3. **Попробуйте добавить вещь и посмотрите логи:**

#### Ожидаемые логи при успешной работе:
```
🚀 Начинаем обработку изображения: {name: "photo.jpg", type: "image/jpeg", size: 1234567}
✅ Валидация файла прошла успешно
📡 Отправляем запрос к backend...
📤 BackendService: Отправляем изображение на анализ
📤 BackendService: URL запроса: http://localhost:5001/analyze-wardrobe-item
📥 BackendService: Получен ответ: {status: 200, ok: true}
✅ BackendService: Успешно получен JSON ответ: {success: true, hasImage: true, hasAnalysis: true}
✅ Обработка завершена успешно, переходим к редактированию
```

#### Возможные ошибки и их решения:

### 🔴 **Ошибка 1: "Failed to fetch"**
```
❌ BackendService: Wardrobe analysis failed: TypeError: Failed to fetch
```
**Решение:** Проблема с сетью или backend недоступен
- Проверьте подключение к интернету
- Убедитесь, что backend запущен на порту 5001
- Проверьте, что мобильное устройство может достучаться до backend

### 🔴 **Ошибка 2: "Load failed"**
```
❌ Image processing failed: Error: Load failed
```
**Решение:** Проблема с загрузкой изображения в canvas
- Попробуйте другое изображение
- Уменьшите размер изображения
- Перезагрузите страницу

### 🔴 **Ошибка 3: "HTTP error! status: 413"**
```
❌ BackendService: HTTP error response: File too large
```
**Решение:** Файл слишком большой
- Выберите изображение меньшего размера
- Сожмите изображение перед загрузкой

### 🔴 **Ошибка 4: "CORS"**
```
❌ BackendService: Wardrobe analysis failed: TypeError: CORS error
```
**Решение:** Проблема с CORS настройками
- Проверьте настройки CORS в backend
- Убедитесь, что домен добавлен в ALLOWED_ORIGINS

## 🛠️ Быстрые исправления:

### 1. **Проверьте backend:**
```bash
# В терминале на компьютере
curl http://localhost:5001/health
```

### 2. **Проверьте конфигурацию:**
- Убедитесь, что `BACKEND_URL` в `src/config.js` правильный
- Для мобильных устройств используйте IP адрес компьютера, а не localhost

### 3. **Обновите BACKEND_URL для мобильных устройств:**
```javascript
// В src/config.js
export const BACKEND_URL = import.meta.env.VITE_BACKEND_URL || 'http://192.168.1.42:5001';
```

## 📱 Тестирование на мобильном:

1. **Откройте приложение** в мобильном браузере
2. **Проверьте статус backend** в левом верхнем углу
3. **Попробуйте добавить вещь** через камеру или галерею
4. **Посмотрите логи** в консоли разработчика
5. **Сообщите точную ошибку** из логов

## 🚨 Если ничего не помогает:

1. **Проверьте, что backend запущен:**
   ```bash
   cd backend
   source venv/bin/activate
   python app.py
   ```

2. **Проверьте, что backend доступен с мобильного:**
   - Откройте в браузере телефона: `http://IP_КОМПЬЮТЕРА:5001/health`
   - Должен вернуться JSON с статусом "ok"

3. **Проверьте файрвол и настройки сети:**
   - Убедитесь, что порт 5001 открыт
   - Проверьте, что устройства в одной сети

## 📞 Сообщите результаты:

При обращении за помощью укажите:
- Точную ошибку из консоли
- Статус backend (из индикатора)
- URL backend (из индикатора)
- Тип устройства и браузер
- Размер и тип тестового изображения 