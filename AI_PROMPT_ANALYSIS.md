# 🤖 Анализ промпта для генерации капсул гардероба

## 📋 Общая структура промпта

Система использует **Ollama + Llama2 (7B)** для генерации персонализированных капсул гардероба.

## 🎯 Основной промпт

```python
prompt = f"""
Ты - эксперт по стилю и модный консультант. Создай капсулы гардероба на основе предоставленных данных.

ГАРДЕРОБ ПОЛЬЗОВАТЕЛЯ:
{wardrobe_text}

ПРОФИЛЬ ПОЛЬЗОВАТЕЛЯ:
{profile_text}

ПОГОДА:
{weather_text}

Создай 6 категорий капсул:
1. Повседневный стиль (casual) - уютные образы для ежедневных дел
2. Деловой образ (business) - элегантные решения для работы  
3. Вечерний выход (evening) - стильные образы для особых случаев
4. Романтическое свидание (romantic) - нежные и привлекательные образы
5. Выходные (weekend) - расслабленные образы для отдыха
6. Путешествия (travel) - практичные образы для поездок

Для каждой категории создай:
- 2 примера образов (по 3-4 вещи)
- 8 полных капсул (по 5-6 вещей)

Правила:
- Учитывай тип фигуры и возраст
- Подбирай вещи по сезону и погоде
- Создавай гармоничные комбинации
- Добавляй объяснение выбора

Верни ответ в формате JSON:
{{
    "categories": [
        {{
            "id": "casual",
            "name": "Повседневный стиль", 
            "description": "Уютные образы для ежедневных дел",
            "examples": [
                {{
                    "id": "casual_1",
                    "name": "Повседневный стиль - Образ 1",
                    "items": ["id_вещи_1", "id_вещи_2", "id_вещи_3"],
                    "description": "Описание образа",
                    "reasoning": "Объяснение выбора"
                }}
            ],
            "fullCapsules": [
                {{
                    "id": "casual_full_1", 
                    "name": "Повседневный стиль - Капсула 1",
                    "items": ["id_вещи_1", "id_вещи_2", "id_вещи_3", "id_вещи_4", "id_вещи_5"],
                    "description": "Описание капсулы",
                    "reasoning": "Объяснение выбора"
                }}
            ]
        }}
    ]
}}
"""
```

## 📊 Входные данные

### 1. Гардероб пользователя
```python
wardrobe_text = "\n".join([
    f"- {item.get('category', 'Неизвестно')}: {item.get('description', '')} (сезон: {item.get('season', 'Круглогодично')})"
    for item in wardrobe
])
```

**Пример:**
```
- Блузка: Белая блузка (сезон: Круглогодично)
- Джинсы: Синие джинсы (сезон: Круглогодично)
- Сумка: Сумка шопер, темно-шоколадного цвета (сезон: Круглогодично)
```

### 2. Профиль пользователя
```python
profile_text = f"""
Имя: {profile.get('name', 'Не указано')}
Возраст: {profile.get('age', 'Не указан')}
Тип фигуры: {profile.get('figura', 'Не указан')}
Цветотип: {profile.get('cvetotip', 'Не указан')}
Любимая зона: {profile.get('like_zone', 'Не указана')}
Проблемная зона: {profile.get('dislike_zone', 'Не указана')}
"""
```

**Пример:**
```
Имя: Анна
Возраст: 25
Тип фигуры: Песочные часы
Цветотип: Весна
Любимая зона: Талия
Проблемная зона: Бедра
```

### 3. Погодные данные
```python
weather_text = ""
if weather and 'main' in weather:
    temp = weather['main'].get('temp', 'Неизвестно')
    description = weather.get('weather', [{}])[0].get('description', 'Неизвестно')
    weather_text = f"Текущая погода: {temp}°C, {description}"
```

**Пример:**
```
Текущая погода: 20°C, переменная облачность
```

## ⚙️ Настройки генерации

### Модель ИИ
- **Модель**: `llama2:7b` (по умолчанию)
- **Клиент**: Ollama

### Параметры генерации
```python
options = {
    'temperature': 0.7,    # Креативность (0.0-1.0)
    'top_p': 0.9,         # Разнообразие ответов
    'max_tokens': 4000    # Максимальная длина ответа
}
```

## 🎨 Структура ответа

### Категории капсул
1. **Повседневный стиль** (casual)
2. **Деловой образ** (business)
3. **Вечерний выход** (evening)
4. **Романтическое свидание** (romantic)
5. **Выходные** (weekend)
6. **Путешествия** (travel)

### Для каждой категории:
- **2 примера образов** (по 3-4 вещи)
- **8 полных капсул** (по 5-6 вещей)

### Структура образа/капсулы:
```json
{
    "id": "casual_1",
    "name": "Повседневный стиль - Образ 1",
    "items": ["id_вещи_1", "id_вещи_2", "id_вещи_3"],
    "description": "Описание образа",
    "reasoning": "Объяснение выбора"
}
```

## 🔄 Fallback система

Если ИИ не работает, используется простая логика:
- Создаются базовые капсулы без ИИ
- Используется простая комбинация вещей из гардероба
- Сохраняется та же структура ответа

## 🎯 Ключевые особенности промпта

### 1. **Персонализация**
- Учитывает тип фигуры
- Учитывает возраст
- Учитывает цветотип
- Учитывает любимые/проблемные зоны

### 2. **Контекстность**
- Учитывает сезонность вещей
- Учитывает текущую погоду
- Учитывает гардероб пользователя

### 3. **Структурированность**
- Четкий формат JSON
- Предопределенные категории
- Детальные описания и объяснения

### 4. **Практичность**
- 6 основных стилей жизни
- Примеры + полные капсулы
- Объяснения выбора

## 🚀 Результат

Система генерирует **48 капсул** (6 категорий × 8 капсул) + **12 примеров** (6 категорий × 2 примера) = **60 вариантов образов** для пользователя. 