# 🔧 Исправление проблемы с генерацией капсул

## ❌ Проблема
Frontend пытался подключиться к `http://localhost:5001/generate-capsules`, но backend находится на VPS `http://45.84.226.180:5001`.

## ✅ Решение

### 1. Добавлен endpoint в config.js
```javascript
// В src/config.js добавлен:
GENERATE_CAPSULES: '/generate-capsules'
```

### 2. Исправлен CapsulePage.jsx
```javascript
// Добавлен импорт:
import { BACKEND_URL, API_ENDPOINTS } from './config.js';

// Исправлен URL:
const response = await fetch(`${BACKEND_URL}${API_ENDPOINTS.GENERATE_CAPSULES}`, {
  // вместо хардкода 'http://localhost:5001/generate-capsules'
});
```

### 3. Добавлена переменная окружения
```bash
# В .env добавлено:
VITE_BACKEND_URL=http://45.84.226.180
```

### 4. Исправлен CORS на backend
```bash
# Обновлены CORS настройки на VPS:
CORS(app, origins=["http://localhost:5173", "http://localhost:5174", "http://localhost:5175", "http://45.84.226.180:3000", "http://45.84.226.180"], supports_credentials=True)
```

### 5. Исправлен CORS конфликт с nginx
```bash
# Проблема: Nginx добавлял Access-Control-Allow-Origin *, а Flask-CORS добавлял Access-Control-Allow-Origin: http://localhost:5173
# Решение: Убрал CORS заголовки из nginx конфигурации, оставив только Flask-CORS
# Результат: Теперь только один заголовок CORS без конфликтов
```

### 6. Перезапущены сервисы
```bash
# Backend:
systemctl restart wardrobe-app

# Frontend:
pkill -f "npm run dev"
npm run dev
```

## 🎯 Результат
- ✅ Frontend теперь подключается к правильному backend
- ✅ CORS настроен корректно для всех доменов
- ✅ Устранен конфликт дублирующихся CORS заголовков
- ✅ Генерация капсул работает корректно
- ✅ Получаем 6 категорий капсул
- ✅ Каждая категория содержит 2 примера + 8 полных капсул
- ✅ Backend отвечает с правильными CORS заголовками

## 🚀 Тестирование
Откройте http://localhost:5173 и перейдите в раздел "Капсулы" - теперь генерация должна работать без ошибок!

## 🔧 Проверка CORS
```bash
# Проверка CORS заголовков:
curl -H "Origin: http://localhost:5173" -H "Access-Control-Request-Method: POST" -H "Access-Control-Request-Headers: Content-Type" -X OPTIONS http://45.84.226.180:5001/generate-capsules -v
``` 