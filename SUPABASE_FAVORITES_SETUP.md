# 🎯 Настройка системы избранного в Supabase

## 📋 Что нужно сделать:

### 1. **Создание таблицы в Supabase**

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Выполните SQL скрипт из файла `SUPABASE_FAVORITES_SETUP.sql`
3. Проверьте, что таблица `favorites` создана в **Table Editor**

### 2. **Структура таблицы `favorites`**

```sql
CREATE TABLE favorites (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  telegram_id TEXT NOT NULL,
  capsule_id TEXT NOT NULL,
  capsule_name TEXT NOT NULL,
  capsule_description TEXT,
  capsule_category TEXT,
  capsule_data JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(telegram_id, capsule_id)
);
```

### 3. **Поля таблицы:**

- **`id`** - уникальный идентификатор записи
- **`telegram_id`** - Telegram ID пользователя
- **`capsule_id`** - уникальный ID капсулы
- **`capsule_name`** - название капсулы
- **`capsule_description`** - описание капсулы
- **`capsule_category`** - категория (casual, business, evening и т.д.)
- **`capsule_data`** - JSON с полными данными капсулы
- **`created_at`** - время создания записи
- **`updated_at`** - время последнего обновления

### 4. **Безопасность (RLS)**

Таблица защищена Row Level Security:
- ✅ Пользователи видят только свои избранные
- ✅ Пользователи могут добавлять только свои избранные
- ✅ Пользователи могут удалять только свои избранные

### 5. **Индексы для производительности**

```sql
CREATE INDEX idx_favorites_telegram_id ON favorites(telegram_id);
CREATE INDEX idx_favorites_category ON favorites(capsule_category);
CREATE INDEX idx_favorites_created_at ON favorites(created_at);
```

## 🚀 Функциональность

### **Добавление в избранное:**
```javascript
await favoritesService.addToFavorites(telegramId, capsule);
```

### **Удаление из избранного:**
```javascript
await favoritesService.removeFromFavorites(telegramId, capsuleId);
```

### **Получение избранного:**
```javascript
const favorites = await favoritesService.getFavorites(telegramId);
```

### **Проверка избранного:**
```javascript
const isFavorite = await favoritesService.isInFavorites(telegramId, capsuleId);
```

### **Статистика:**
```javascript
const stats = await favoritesService.getFavoritesStats(telegramId);
// { total: 15, byCategory: { casual: 8, business: 4, evening: 3 } }
```

## 🔄 Синхронизация

### **Преимущества Supabase:**
- ✅ **Синхронизация между устройствами** - избранное доступно везде
- ✅ **Синхронизация между браузерами** - работает в любом браузере
- ✅ **Резервное копирование** - данные не теряются
- ✅ **Безопасность** - RLS защищает данные пользователей

### **Fallback система:**
- Если Supabase недоступен, используется localStorage
- Данные автоматически синхронизируются при восстановлении связи

## 🧪 Тестирование

### **1. Проверка создания таблицы:**
```sql
SELECT * FROM favorites LIMIT 5;
```

### **2. Проверка RLS политик:**
```sql
-- Должно вернуть только записи текущего пользователя
SELECT * FROM favorites WHERE telegram_id = 'your_telegram_id';
```

### **3. Тестирование в приложении:**
1. Откройте приложение
2. Перейдите в капсулы
3. Добавьте капсулу в избранное
4. Проверьте, что она появилась в разделе "Избранное"
5. Откройте в другом браузере - избранное должно быть там

## 🔧 Возможные проблемы:

### **Ошибка "relation does not exist":**
- Проверьте, что SQL скрипт выполнен полностью
- Убедитесь, что таблица создана в правильной схеме

### **Ошибка "permission denied":**
- Проверьте RLS политики
- Убедитесь, что `telegram_id` передается корректно

### **Данные не синхронизируются:**
- Проверьте подключение к Supabase
- Убедитесь, что API ключи корректны

## 📱 Результат:

После настройки пользователи смогут:
- ✅ Добавлять капсулы в избранное
- ✅ Видеть избранное на любом устройстве
- ✅ Синхронизировать избранное между браузерами
- ✅ Получать статистику по избранному
- ✅ Безопасно хранить данные

## 🎯 Следующие шаги:

1. Выполните SQL скрипт в Supabase
2. Протестируйте функциональность
3. Проверьте синхронизацию между устройствами
4. При необходимости настройте дополнительные индексы 